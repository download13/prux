const jsdom = require('mocha-jsdom');


describe('Component', () => {
	jsdom();

	xit('can be created from a function', () => {
		// TODO: simple
	});

	xit('can be created from a spec with state', () => {
		// TODO: counter
	});

	xit('can be created with lifecycle', () => {
		// TODO: clock
	});

	xit('can respond to props changes', () => {
		// TODO: simple
	});

	xit('can respond to attribute changes', () => {
		// TODO: simple
	});

	xit('can respond to state changes', () => {
		// TODO: counter
	});

	xit('can respond to lifecycle notifications', () => {
		// TODO: clock
	});
});
